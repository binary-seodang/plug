{
  "name" : "database",
  "version": "0.0.1",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "db:generate" : "prisma generate",
    "db:push" : "npx prisma db push --skip-generate"
  },
  "dependencies": {
    "@prisma/client": "^4.9.0"
  },
  "devDependencies": {
    "prisma": "^4.9.0"
  }
}